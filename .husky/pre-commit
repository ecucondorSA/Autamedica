#!/bin/sh
. "$(dirname "$0")/_/husky.sh"

echo "🔎 Verificando glosarios antes del commit..."

# DB Glossary (modo CI: parsing SQL, sin DB)
pnpm docs:db:ci || exit 1
pnpm docs:db:check-diff || exit 1

# Glosario de contratos (types)
pnpm docs:validate || exit 1

echo "✅ Glosarios OK"
