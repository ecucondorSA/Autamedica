# 🐳 AltaMedica - Docker Environment Variables
# Copiar este archivo a .env.docker y ajustar valores según sea necesario

# ==========================================
# 🔐 POSTGRES CONFIGURATION
# ==========================================
POSTGRES_PASSWORD=your-super-secret-and-long-postgres-password

# ==========================================
# 🔑 JWT CONFIGURATION
# ==========================================
# IMPORTANTE: Generar con: openssl rand -base64 32
JWT_SECRET=your-super-secret-jwt-token-with-at-least-32-characters-long
JWT_EXPIRY=3600

# ==========================================
# 🔓 SUPABASE KEYS (Development Only)
# ==========================================
# ANON KEY - Para uso público (frontend)
ANON_KEY=eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.eyJpc3MiOiJzdXBhYmFzZS1kZW1vIiwicm9sZSI6ImFub24iLCJleHAiOjE5ODM4MTI5OTZ9.CRXP1A7WOeoJeXxjNni43kdQwgnWNReilDMblYTn_I0

# SERVICE ROLE KEY - Para uso administrativo (backend)
SERVICE_ROLE_KEY=eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.eyJpc3MiOiJzdXBhYmFzZS1kZW1vIiwicm9sZSI6InNlcnZpY2Vfcm9sZSIsImV4cCI6MTk4MzgxMjk5Nn0.EGIM96RAZx35lJzdJsyH-qQwv8Hdp7fsn3W0YpN81IU

# ==========================================
# 🌐 URLs & DOMAINS
# ==========================================
# URL externa de la API (accesible desde frontend)
API_EXTERNAL_URL=http://localhost:8000

# URL pública de Supabase (para emails, callbacks, etc.)
SUPABASE_PUBLIC_URL=http://localhost:8000

# URL base de la aplicación
SITE_URL=http://localhost:3000

# URLs adicionales permitidas para redirección OAuth
ADDITIONAL_REDIRECT_URLS=

# URL base para Playwright tests
BASE_URL=http://host.docker.internal:3000

# ==========================================
# 📧 EMAIL CONFIGURATION (Inbucket Local)
# ==========================================
SMTP_HOST=inbucket
SMTP_PORT=2500
SMTP_ADMIN_EMAIL=admin@altamedica.local
SMTP_SENDER_NAME=AltaMedica Auth

# Rutas URL para emails
MAILER_URLPATHS_INVITE=/auth/v1/verify
MAILER_URLPATHS_CONFIRMATION=/auth/v1/verify
MAILER_URLPATHS_RECOVERY=/auth/v1/verify
MAILER_URLPATHS_EMAIL_CHANGE=/auth/v1/verify

# ==========================================
# 🔒 AUTH CONFIGURATION
# ==========================================
# Habilitar registro por email
ENABLE_EMAIL_SIGNUP=true

# Auto-confirmar emails (solo desarrollo)
ENABLE_EMAIL_AUTOCONFIRM=true

# Deshabilitar registro completamente
DISABLE_SIGNUP=false

# Habilitar autenticación por teléfono
ENABLE_PHONE_SIGNUP=false
ENABLE_PHONE_AUTOCONFIRM=false

# ==========================================
# 🗄️ DATABASE SCHEMAS
# ==========================================
PGRST_DB_SCHEMAS=public,storage,graphql_public

# ==========================================
# 🖼️ IMAGE PROCESSING
# ==========================================
IMGPROXY_ENABLE_WEBP_DETECTION=true

# ==========================================
# 🎨 STUDIO CONFIGURATION
# ==========================================
STUDIO_DEFAULT_ORGANIZATION=AltaMedica
STUDIO_DEFAULT_PROJECT=AltaMedica Development

# Credenciales del dashboard (CAMBIAR EN PRODUCCIÓN)
DASHBOARD_USERNAME=supabase
DASHBOARD_PASSWORD=this_password_is_insecure_and_should_be_updated

# ==========================================
# 🎭 PLAYWRIGHT CONFIGURATION
# ==========================================
# Habilitar debug de Playwright
PWDEBUG=0

# ==========================================
# 🛠️ NOTAS DE CONFIGURACIÓN
# ==========================================
#
# 1. JWT_SECRET: Debe ser al menos 32 caracteres. Generar con:
#    openssl rand -base64 32
#
# 2. POSTGRES_PASSWORD: Cambiar en producción a una contraseña segura
#
# 3. ANON_KEY y SERVICE_ROLE_KEY: Para desarrollo local están OK.
#    En producción, generar nuevos keys con el JWT_SECRET correspondiente
#
# 4. ENABLE_EMAIL_AUTOCONFIRM=true: Solo para desarrollo.
#    En producción debe ser false.
#
# 5. DASHBOARD_PASSWORD: SIEMPRE cambiar en producción
#
# 6. BASE_URL: Para Playwright en Docker, usar host.docker.internal
#    para apuntar al localhost del host
#
