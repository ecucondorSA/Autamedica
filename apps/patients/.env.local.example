# ====================================================================
# AutaMédica Patients App – .env.local (ejemplo)
# Copia este archivo a .env.local y rellena los valores reales
# ====================================================================

# 🌐 URLs de Aplicaciones (si usas multi-dominio)
NEXT_PUBLIC_APP_URL=http://localhost:3002
NEXT_PUBLIC_PATIENTS_URL=http://localhost:3002
NEXT_PUBLIC_DOCTORS_URL=http://localhost:3004
NEXT_PUBLIC_COMPANIES_URL=http://localhost:3005

# 🔗 Base URL para callbacks de autenticación
NEXT_PUBLIC_BASE_URL=http://localhost:3002
NEXT_PUBLIC_AUTH_CALLBACK_URL=http://localhost:3002/auth/callback

# 🔐 Supabase (Server-Side)
SUPABASE_URL=https://YOUR_PROJECT_REF.supabase.co
SUPABASE_ANON_KEY=eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.YOUR_ANON
SUPABASE_SERVICE_ROLE_KEY=eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.YOUR_SERVICE_ROLE

# 🔐 Supabase (Client-Side)
NEXT_PUBLIC_SUPABASE_URL=https://YOUR_PROJECT_REF.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.YOUR_ANON

# 🍪 Auth multi-dominio (opcional en local)
AUTH_COOKIE_DOMAIN=localhost

# ⚙️ Next.js
NODE_ENV=development
PORT=3002

# 🩺 Feature flags
NEXT_PUBLIC_PATIENT_PORTAL_ENABLED=true
NEXT_PUBLIC_TELEMEDICINE_ENABLED=true
NEXT_PUBLIC_PRESCRIPTION_MANAGEMENT_ENABLED=true
NEXT_PUBLIC_USE_LIVEKIT=0
# Alias legacy aceptado (1/true). Puedes eliminarlo si no se usa.
NEXT_PUBLIC_LIVEKIT=0
# Backend de signaling (LiveKit / Node). No se expone al cliente.
SIGNALING_SERVICE_URL=http://localhost:8888
# Opcional: expone la URL al cliente si necesitas conexiones directas.
# NEXT_PUBLIC_SIGNALING_SERVICE_URL=http://localhost:8888

# 📊 Monitoring (opcional)
NEXT_PUBLIC_SENTRY_DSN=

# 🤖 Auta ONNX en navegador (opcional)
# Activa la carga del modelo ONNX en el cliente. Si está en 1/true/on,
# se intentará cargar el modelo desde la ruta indicada abajo.
NEXT_PUBLIC_AUTA_ONNX=0
# Ruta pública del modelo (sirve desde /public). Ej: /models/intent.onnx
NEXT_PUBLIC_AUTA_ONNX_MODEL=/models/intent.onnx
